<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MLKitPOC"
        tools:targetApi="31">
        <activity
            android:name=".integration.IntegrationListActivity"
            android:exported="false" />
        <activity
            android:name=".integration.IntegrationImageUploadActivity"
            android:exported="false" />
        <activity
            android:name=".integration.IntegrationMainActivity"
            android:exported="false" />
        <activity
            android:name=".launch.MainLaunchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        Optional: Add it to automatically download ML model to device after
         your app is installed.
        -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr,ica" />

        <activity
            android:name=".main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".list.ListActivity"
            android:exported="false" />
        <activity
            android:name=".image.ImageUploadActivity"
            android:exported="false" />
        <activity
            android:name=".image.ImageDisplayActivity"
            android:exported="false" />
    </application>

</manifest>